# https://git-scm.com/docs/git-config
[alias]
    ad = add
    ada = add --all .
    bl = blame
    br = branch
    bra = branch --all
    brd = branch --delete
    ch = checkout
    cl = clone
    cm = commit
    cmal = commit --all
    cmam = commit --amend
    cmm = commit -m
    co = config
    cp = cherry-pick
    cpa = cherry-pick --abort
    di = diff --ignore-cr-at-eol
    dit = difftool
    diw = diff --word-diff
    fe = fetch
    fea = fetch --all
    fst = !git fetch --all && git status
    he = help
    lfm = ls-files -m
    lo = log
    loo = log --oneline
    lsf = ls-files
    me = merge
    mea = merge --abort
    pl = pull
    ps = push
    psf = push --force-with-lease
    psu = push --set-upstream
    rb = rebase
    rba = rebase --abort
    rbi = rebase --interactive
    rf = reflog
    rs = reset
    rts = restore --staged
    rv = revert
    ss = stash
    ssa = stash apply
    ssc = stash clear
    ssd = stash drop
    ssl = stash list
    ssp = stash pop
    sss = stash save
    st = status --short
    tree = log --all --decorate --full-history --graph --oneline
[color]
    ui = true
[core]
    autocrlf = input
    editor = vim -c 'startinsert'  # code --wait, nano
    eol = native
    filemode = false
    fscache = true
    hideDotFiles = false
    preloadindex = true
[credential]
    helper =
[credential "helperselector"]
    selected = <no helper>
[diff]
    algorithm = histogram
    tool = vimdiff
[gc]
    auto = 1024
[merge]
    conflictstyle = zdiff3
    tool = vimdiff
[pull]
    rebase = false  # false for fast-forward/recursive, merges to auto-rebase
[push]
    autoSetupRemote = true
    default = simple
[rerere]
    enabled = true
[user]
    email =
    name =
